# CLI Chat Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现一个极简的 streaming CLI (`src/cli/chat.ts`)，支持多轮对话、thinking 流式输出、工具执行（read / bash / edit / write），用于直接调试 `Agent`。

**Architecture:** CLI 直接实例化 `Agent` 并订阅其事件流；事件处理器将 `text_delta` / `thinking_delta` / `tool_execution_*` 等事件映射为终端输出；本地 slash 命令在主循环中拦截处理。工具层提供最小可用的文件读写、bash 执行实现。

**Tech Stack:** Bun, TypeScript, `readline/promises`, `chalk`, `fs/promises`, `child_process`

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `src/tools/read.ts` | 读取文件内容（支持 offset / limit） |
| `src/tools/write.ts` | 写入文件（自动创建目录） |
| `src/tools/edit.ts` | 基于 oldText → newText 的文本替换编辑 |
| `src/tools/bash.ts` | 执行本地 shell 命令 |
| `src/tools/index.ts` | 统一导出四个工具 |
| `src/cli/chat.ts` | CLI 主入口：Agent 初始化、事件渲染、输入循环、slash 命令 |

---

### Task 1: 创建 `read` 工具

**Files:**
- Create: `src/tools/read.ts`

- [ ] **Step 1: 实现 read 工具**

```typescript
// src/tools/read.ts
import { Type, type Static } from "@sinclair/typebox";
import { readFile } from "fs/promises";
import { resolve } from "path";
import type { AgentTool } from "../agent/index.js";

const readSchema = Type.Object({
  path: Type.String({ description: "Path to the file to read (relative or absolute)" }),
  offset: Type.Optional(Type.Number({ description: "Line number to start reading from (1-indexed)" })),
  limit: Type.Optional(Type.Number({ description: "Maximum number of lines to read" })),
});

type ReadInput = Static<typeof readSchema>;

export function createReadTool(cwd: string): AgentTool<typeof readSchema> {
  return {
    name: "read",
    label: "Read",
    description: "Read the contents of a file.",
    parameters: readSchema,
    async execute(toolCallId, params) {
      const absolutePath = resolve(cwd, params.path);
      const buffer = await readFile(absolutePath);
      let text = buffer.toString("utf-8");

      if (params.offset !== undefined || params.limit !== undefined) {
        const lines = text.split("\n");
        const start = Math.max(0, (params.offset ?? 1) - 1);
        const end = params.limit !== undefined ? start + params.limit : lines.length;
        text = lines.slice(start, end).join("\n");
      }

      return {
        content: [{ type: "text", text }],
        details: { path: absolutePath },
      };
    },
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/tools/read.ts
git commit -m "feat(tools): add read tool"
```

---

### Task 2: 创建 `write` 工具

**Files:**
- Create: `src/tools/write.ts`

- [ ] **Step 1: 实现 write 工具**

```typescript
// src/tools/write.ts
import { Type, type Static } from "@sinclair/typebox";
import { mkdir, writeFile } from "fs/promises";
import { dirname, resolve } from "path";
import type { AgentTool } from "../agent/index.js";

const writeSchema = Type.Object({
  path: Type.String({ description: "Path to the file to write (relative or absolute)" }),
  content: Type.String({ description: "Content to write to the file" }),
});

type WriteInput = Static<typeof writeSchema>;

export function createWriteTool(cwd: string): AgentTool<typeof writeSchema> {
  return {
    name: "write",
    label: "Write",
    description: "Write content to a file. Creates parent directories if needed.",
    parameters: writeSchema,
    async execute(toolCallId, params) {
      const absolutePath = resolve(cwd, params.path);
      await mkdir(dirname(absolutePath), { recursive: true });
      await writeFile(absolutePath, params.content, "utf-8");

      return {
        content: [{ type: "text", text: `Wrote ${params.content.length} bytes to ${absolutePath}` }],
        details: { path: absolutePath, bytes: params.content.length },
      };
    },
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/tools/write.ts
git commit -m "feat(tools): add write tool"
```

---

### Task 3: 创建 `edit` 工具

**Files:**
- Create: `src/tools/edit.ts`

- [ ] **Step 1: 实现 edit 工具**

```typescript
// src/tools/edit.ts
import { Type, type Static } from "@sinclair/typebox";
import { readFile, writeFile } from "fs/promises";
import { resolve } from "path";
import type { AgentTool } from "../agent/index.js";

const replaceEditSchema = Type.Object({
  oldText: Type.String({ description: "Exact text to replace" }),
  newText: Type.String({ description: "Replacement text" }),
});

const editSchema = Type.Object({
  path: Type.String({ description: "Path to the file to edit (relative or absolute)" }),
  edits: Type.Array(replaceEditSchema, {
    description: "One or more targeted replacements. oldText must be unique in the file.",
  }),
});

type EditInput = Static<typeof editSchema>;

export function createEditTool(cwd: string): AgentTool<typeof editSchema> {
  return {
    name: "edit",
    label: "Edit",
    description: "Edit a file by replacing exact text segments.",
    parameters: editSchema,
    async execute(toolCallId, params) {
      const absolutePath = resolve(cwd, params.path);
      let content = await readFile(absolutePath, "utf-8");

      for (const edit of params.edits) {
        if (!content.includes(edit.oldText)) {
          throw new Error(`oldText not found in file: ${edit.oldText.slice(0, 50)}...`);
        }
        const occurrences = content.split(edit.oldText).length - 1;
        if (occurrences > 1) {
          throw new Error(`oldText is not unique in file (found ${occurrences} occurrences)`);
        }
        content = content.replace(edit.oldText, edit.newText);
      }

      await writeFile(absolutePath, content, "utf-8");

      return {
        content: [{ type: "text", text: `Edited ${absolutePath} with ${params.edits.length} replacement(s)` }],
        details: { path: absolutePath, edits: params.edits.length },
      };
    },
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/tools/edit.ts
git commit -m "feat(tools): add edit tool"
```

---

### Task 4: 创建 `bash` 工具

**Files:**
- Create: `src/tools/bash.ts`

- [ ] **Step 1: 实现 bash 工具**

```typescript
// src/tools/bash.ts
import { Type, type Static } from "@sinclair/typebox";
import { spawn } from "child_process";
import type { AgentTool } from "../agent/index.js";

const bashSchema = Type.Object({
  command: Type.String({ description: "Bash command to execute" }),
  timeout: Type.Optional(Type.Number({ description: "Timeout in seconds (optional)" })),
});

type BashInput = Static<typeof bashSchema>;

export function createBashTool(cwd: string): AgentTool<typeof bashSchema> {
  return {
    name: "bash",
    label: "Bash",
    description: "Execute a bash command in the working directory.",
    parameters: bashSchema,
    async execute(toolCallId, params, signal) {
      return new Promise((resolve, reject) => {
        const child = spawn("bash", ["-c", params.command], { cwd });
        const stdoutChunks: Buffer[] = [];
        const stderrChunks: Buffer[] = [];

        let timeoutId: ReturnType<typeof setTimeout> | undefined;
        if (params.timeout) {
          timeoutId = setTimeout(() => {
            child.kill("SIGTERM");
          }, params.timeout * 1000);
        }

        child.stdout.on("data", (data: Buffer) => stdoutChunks.push(data));
        child.stderr.on("data", (data: Buffer) => stderrChunks.push(data));

        child.on("error", (err) => {
          if (timeoutId) clearTimeout(timeoutId);
          reject(err);
        });

        child.on("close", (code) => {
          if (timeoutId) clearTimeout(timeoutId);
          if (signal?.aborted) {
            reject(new Error("Aborted"));
            return;
          }
          const stdout = Buffer.concat(stdoutChunks).toString("utf-8");
          const stderr = Buffer.concat(stderrChunks).toString("utf-8");
          const output = stdout + (stderr ? `\nstderr:\n${stderr}` : "");
          const isError = code !== 0;

          resolve({
            content: [{ type: "text", text: output || "(no output)" }],
            details: { exitCode: code, command: params.command },
          });
        });
      });
    },
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/tools/bash.ts
git commit -m "feat(tools): add bash tool"
```

---

### Task 5: 创建工具统一导出

**Files:**
- Create: `src/tools/index.ts`

- [ ] **Step 1: 编写 index.ts**

```typescript
// src/tools/index.ts
export { createReadTool } from "./read.js";
export { createWriteTool } from "./write.js";
export { createEditTool } from "./edit.js";
export { createBashTool } from "./bash.js";
```

- [ ] **Step 2: Commit**

```bash
git add src/tools/index.ts
git commit -m "feat(tools): export all coding tools"
```

---

### Task 6: 实现 `src/cli/chat.ts` CLI 主入口

**Files:**
- Create: `src/cli/chat.ts`

- [ ] **Step 1: 编写 chat.ts**

```typescript
// src/cli/chat.ts
import { createInterface } from "readline/promises";
import chalk from "chalk";
import { Agent } from "../agent/index.js";
import type { AgentEvent, AgentMessage } from "../agent/index.js";
import { getEnvApiKey, getModel } from "../core/ai/index.js";
import { createBashTool, createEditTool, createReadTool, createWriteTool } from "../tools/index.js";

const cwd = process.cwd();
const systemPrompt = process.argv[2] || "You are a helpful coding assistant.";

const model = getModel("minimax", "MiniMax-M2.7");
if (!model) {
  console.error("Model not found");
  process.exit(1);
}

const apiKey = getEnvApiKey(model.provider);
if (!apiKey) {
  console.error(`Missing API key for provider: ${model.provider}. Set ${model.provider === "minimax-cn" ? "MINIMAX_CN_API_KEY" : "MINIMAX_API_KEY"}`);
  process.exit(1);
}

const agent = new Agent({
  initialState: {
    systemPrompt,
    model,
    thinkingLevel: "medium",
    tools: [createReadTool(cwd), createWriteTool(cwd), createEditTool(cwd), createBashTool(cwd)],
  },
  getApiKey: () => apiKey,
});

let isStreaming = false;

agent.subscribe(async (event) => {
  handleEvent(event);
});

function handleEvent(event: AgentEvent): void {
  switch (event.type) {
    case "agent_start":
      isStreaming = true;
      process.stdout.write(chalk.dim("▶ "));
      break;

    case "agent_end":
      isStreaming = false;
      console.log();
      break;

    case "text_delta": {
      const assistantEvent = (event as any).assistantMessageEvent;
      if (assistantEvent?.type === "text_delta") {
        process.stdout.write(assistantEvent.delta);
      }
      break;
    }

    case "thinking_delta": {
      const assistantEvent = (event as any).assistantMessageEvent;
      if (assistantEvent?.type === "thinking_delta") {
        process.stdout.write(chalk.gray(assistantEvent.delta));
      }
      break;
    }

    case "tool_execution_start":
      console.log(`\n🔧 ${event.toolName}`);
      break;

    case "tool_execution_end": {
      const toolResult = event.result as { content: Array<{ type: "text"; text: string }> };
      const text = toolResult?.content?.[0]?.text ?? "";
      const prefix = event.isError ? chalk.red("✗") : chalk.green("✓");
      console.log(`${prefix} ${text.slice(0, 200)}${text.length > 200 ? "..." : ""}`);
      break;
    }

    case "turn_end": {
      const msg = event.message;
      if (msg.role === "assistant" && msg.usage) {
        console.log(
          chalk.dim(
            `\n[Tokens: ${msg.usage.totalTokens} | Cost: $${msg.usage.cost.total.toFixed(4)}]`
          )
        );
      }
      break;
    }
  }
}

const rl = createInterface({
  input: process.stdin,
  output: process.stdout,
});

async function main() {
  console.log(chalk.bold("ys-code chat CLI"));
  console.log(chalk.dim(`Model: ${model.name} | Thinking: medium | Tools: read, write, edit, bash`));
  console.log(chalk.dim("Commands: /exit, /new, /system, /tools, /messages, /abort"));
  console.log();

  while (true) {
    const input = await rl.question("> ");
    const text = input.trim();
    if (!text) continue;

    if (text.startsWith("/")) {
      const handled = await handleCommand(text);
      if (handled) continue;
    }

    if (isStreaming) {
      agent.steer({ role: "user", content: [{ type: "text", text }], timestamp: Date.now() });
    } else {
      await agent.prompt(text);
    }
  }
}

async function handleCommand(text: string): Promise<boolean> {
  switch (text) {
    case "/exit":
      rl.close();
      process.exit(0);

    case "/new":
      agent.reset();
      console.log(chalk.dim("Session cleared."));
      return true;

    case "/system":
      console.log(chalk.dim("System prompt:"));
      console.log(agent.state.systemPrompt);
      return true;

    case "/tools":
      console.log(chalk.dim("Active tools:"));
      for (const tool of agent.state.tools) {
        console.log(`  - ${tool.name}`);
      }
      return true;

    case "/messages":
      console.log(JSON.stringify(agent.state.messages, null, 2));
      return true;

    case "/abort":
      agent.abort();
      return true;

    default:
      return false;
  }
}

main().catch((err) => {
  console.error(chalk.red(err.message));
  process.exit(1);
});
```

**注意**：上述代码中对 `text_delta` / `thinking_delta` 的读取方式可能需要调整。`AgentEvent` 中的 `message_update` 事件携带 `assistantMessageEvent`，而 `text_delta` / `thinking_delta` 是直接作为 `AgentEvent`  union 的一部分还是嵌套在 `message_update` 里，需要以 `src/agent/types.ts` 的定义为准。当前 `AgentEvent` 里只有 `message_update` 包含 `assistantMessageEvent`，但 `agent-loop.ts` 内部可能会把流事件直接转成 `text_delta` / `thinking_delta` 类型的 `AgentEvent`。如果编译报错，请在 `handleEvent` 里统一监听 `message_update`，并从中提取 `assistantMessageEvent` 的 `type` 为 `text_delta` / `thinking_delta` 的情况。

修正后的 `handleEvent` 片段（如果直接监听 `text_delta` / `thinking_delta` 不工作）：

```typescript
case "message_update": {
  const ae = event.assistantMessageEvent;
  if (ae.type === "text_delta") {
    process.stdout.write(ae.delta);
  } else if (ae.type === "thinking_delta") {
    process.stdout.write(chalk.gray(ae.delta));
  }
  break;
}
```

- [ ] **Step 2: Type check**

Run: `bun run typecheck`
Expected: 无报错（如果 `AgentEvent` 中没有独立的 `text_delta` / `thinking_delta`，请按 Step 1 末尾的修正方案调整）

- [ ] **Step 3: Commit**

```bash
git add src/cli/chat.ts
git commit -m "feat(cli): add streaming chat entry point"
```

---

## Self-Review

**1. Spec coverage:**
- `read` / `bash` / `edit` / `write` 工具 → Task 1-4
- 工具统一导出 → Task 5
- CLI 入口 `src/cli/chat.ts` → Task 6
- 启动参数 system prompt → Task 6 (`process.argv[2]`)
- thinking level 写死 medium → Task 6
- slash 命令 (/exit, /new, /system, /tools, /messages, /abort) → Task 6
- streaming 输出 (text / thinking / tool) → Task 6
- steering 机制 → Task 6 (`agent.steer`)
- 无持久化 → 未使用任何存储逻辑

**2. Placeholder scan:** 无 TBD / TODO / "implement later"。

**3. Type consistency：** `AgentEvent` 的处理在 Task 6 中做了兼容性说明，确保与 `src/agent/types.ts` 的实际定义对齐。
